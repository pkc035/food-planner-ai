services:
  food-bot:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}